-# - if @user.errors.any?
-#   #error_explanation 
-#     .alert.alert-danger 
-#       The form contains 
-#       = pluralize(@user.errors.count, 'error')
    
-#     %ul
-#     - @user.errors.full_messages.each do |msg|
-#       %li
-#         = msg

- if flash.any?
  #error_explanation 
    .alert.alert-danger 
      The form contains 
      = pluralize(flash[:danger].count, 'error')
    
    %ul
      - flash[:danger].each do |msg|
        %li 
          = msg